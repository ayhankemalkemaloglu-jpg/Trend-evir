# AGENTS.md

## Cursor Cloud specific instructions

This is currently an empty/greenfield repository with no application code, dependencies, or services. The only file is `README.md`.

### Current state
- **Repository:** Trend-evir
- **Branch:** `main`
- **Contents:** `README.md` only (no source code, no dependencies, no configuration)

### Development environment
- No package manager, build tool, or runtime is configured yet.
- No lint, test, or build commands exist.
- No services need to be started.

### Notes for future agents
- Once application code is added, update this file with setup instructions, service descriptions, and testing guidance.
- Update the VM environment script (`SetupVmEnvironment`) to install the appropriate dependencies when a package manager / dependency file is introduced.
